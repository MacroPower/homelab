hcloud_token = "${HCLOUD_TOKEN}"

cloudflare_api_token = "${CLOUDFLARE_API_TOKEN}"

public_keys_openssh = {
  personal = "${PERSONAL_PUBLIC_KEY_OPENSSH}"
}

tailscale_auth_key = "${TAILSCALE_AUTH_KEY}"
