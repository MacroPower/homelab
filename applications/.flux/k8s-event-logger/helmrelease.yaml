apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: k8s-event-logger
  namespace: kube-system
spec:
  chart:
    spec:
      chart: k8s-event-logger
      version: "1.1"
      sourceRef:
        kind: HelmRepository
        name: https://charts.deliveryhero.io/
  values: {}
