---
# yaml-language-server: $schema=https://json.schemastore.org/chart.json

apiVersion: v2
name: tenant
version: 0.1.0
dependencies:
  - name: project
    version: 0.1.0
    repository: file://../project
