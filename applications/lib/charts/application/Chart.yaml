---
# yaml-language-server: $schema=https://json.schemastore.org/chart.json

apiVersion: v2
name: helm-guestbook
version: 0.0.0 # This value is ignored.
dependencies:
  - name: config
    version: 0.1.0
    repository: file://../config
  - name: policy
    version: 0.1.0
    repository: file://../policy
