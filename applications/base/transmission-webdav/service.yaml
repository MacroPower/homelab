apiVersion: v1
kind: Service
metadata:
  name: transmission-webdav-rclone
spec:
  selector:
    app.kubernetes.io/name: rclone
    app.kubernetes.io/instance: transmission-webdav
  ports:
    - protocol: TCP
      port: 50000
      targetPort: webdav
  type: ClusterIP
