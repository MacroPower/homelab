application:
  syncPolicy:
    automated:
      prune: false

template:
  ingress:
    main:
      enabled: true
      hosts:
        - host: guestbook.home.macro.network
          paths:
            - path: /
