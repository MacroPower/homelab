apiVersion: v1
kind: LimitRange
metadata:
  name: argocd-limit-range
  namespace: argocd
spec:
  limits:
    - default:
        memory: 128Mi
      defaultRequest:
        memory: 128Mi
        cpu: 50m
      type: Container
