nameOverride: tv

configSecretName: transmission-tv-config

persistence:
  enabled: true
  existingClaim: tv

service:
  peer:
    type: LoadBalancer
    ports:
      tcp: 51401
      udp: 51401

flood:
  enabled: true

  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      memory: 512Mi

resources:
  requests:
    cpu: 500m
    memory: 4096Mi
  limits:
    memory: 4096Mi
