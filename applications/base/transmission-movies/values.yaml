nameOverride: movies

configSecretName: transmission-movies-config

persistence:
  enabled: true
  existingClaim: movies

service:
  peer:
    type: LoadBalancer
    ports:
      tcp: 51402
      udp: 51402

flood:
  enabled: true

  resources:
    requests:
      cpu: 50m
      memory: 512Mi
    limits:
      memory: 512Mi

resources:
  requests:
    cpu: 250m
    memory: 4096Mi
  limits:
    memory: 4096Mi
