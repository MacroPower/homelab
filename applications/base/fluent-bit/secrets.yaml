apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: fluent-bit-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: default

  target:
    name: fluent-bit-credentials

  dataFrom:
    - find:
        path: GRAFANA_CLOUD_LOKI_
