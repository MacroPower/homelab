apiVersion: v1
kind: Service
metadata:
  name: twitch-channel-points-miner
spec:
  selector:
    app: twitch-channel-points-miner
  ports:
    - protocol: 'TCP'
      port: 5000
      targetPort: http-analytics
  type: ClusterIP
