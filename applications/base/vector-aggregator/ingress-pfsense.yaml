---
apiVersion: traefik.io/v1alpha1
kind: IngressRouteUDP
metadata:
  name: pfsense-syslog
  annotations:
    external-dns.alpha.kubernetes.io/target: pfsense-syslog.home.macro.network
    external-dns.alpha.kubernetes.io/hostname: pfsense-syslog.home.macro.network
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - pfsensesyslog
  routes:
    - services:
        - name: vector-aggregator
          port: 5140
          weight: 10
          nativeLB: true
