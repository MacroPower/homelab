provider:
  packages:
    - crossplane/provider-sql:v0.5.0
    - xpkg.upbound.io/upbound/provider-terraform:v0.7.0

configuration:
  packages: []

webhooks:
  enabled: true

rbacManager:
  deploy: true

metrics:
  enabled: true
