apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: grafana-cloud-tracing-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: default

  target:
    name: grafana-cloud-tracing-credentials

  dataFrom:
    - find:
        path: GRAFANA_CLOUD_TEMPO_
