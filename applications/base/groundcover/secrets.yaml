---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: groundcover-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: default

  target:
    name: groundcover-credentials

  data:
    - secretKey: token
      remoteRef:
        key: GROUNDCOVER_TOKEN
