global:
  groundcoverPredefinedTokenSecret:
    secretName: groundcover-credentials
    secretKey: token

clusterId: home.macro.network

agent:
  alligator:
    env:
      - name: FLORA_KERNELPROBER_FORCEPMUPROBES
        value: "true"
