apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: twitch-predictions-credentials
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: default

  target:
    name: twitch-predictions-credentials

  dataFrom:
    - find:
        path: TWITCH_PREDICTIONS_
