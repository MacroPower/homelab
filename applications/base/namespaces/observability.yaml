apiVersion: v1
kind: Namespace
metadata:
  name: observability
  labels:
    goldilocks.fairwinds.com/enabled: 'true'

---
apiVersion: v1
kind: LimitRange
metadata:
  name: observability-limit-range
  namespace: observability
spec:
  limits:
    - default:
        memory: 128Mi
      defaultRequest:
        memory: 64Mi
        cpu: 10m
      type: Container
