apiVersion: v1
kind: Namespace
metadata:
  name: automation
  labels:
    goldilocks.fairwinds.com/enabled: 'true'

---
apiVersion: v1
kind: LimitRange
metadata:
  name: automation-limit-range
  namespace: automation
spec:
  limits:
    - default:
        memory: 128Mi
      defaultRequest:
        memory: 64Mi
        cpu: 10m
      type: Container
