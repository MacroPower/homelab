recommender:
  image:
    tag: 0.12.0

  extraArgs:
    prometheus-address: |
      http://kube-prometheus-stack-prometheus.observability.svc.cluster.local:9090
    storage: prometheus

updater:
  enabled: false
  image:
    tag: 0.12.0

admissionController:
  enabled: false
