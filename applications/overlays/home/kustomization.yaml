apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: application-params
    envs:
      - .params

replacements:
  - path: replacement.yaml

resources:
  - goldilocks
