apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: transmission-movies-ingress
  namespace: media
  annotations:
    'external-dns.alpha.kubernetes.io/hostname': 'transmission-movies.macro.network'
    'external-dns.alpha.kubernetes.io/cloudflare-proxied': 'true'
    'traefik.ingress.kubernetes.io/router.entrypoints': 'websecure'
spec:
  tls:
    - hosts:
        - transmission-movies.macro.network
  rules:
    - host: transmission-movies.macro.network
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: transmission-movies
                port:
                  number: 80
