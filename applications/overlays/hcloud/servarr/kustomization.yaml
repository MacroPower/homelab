apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/servarr

patches:
  - target:
      group: charts.kubealex.com
      version: v1
      kind: K8SMediaserver
      name: mediaserver
    patch: |-
      - op: replace
        path: /spec/general/storage/pvcStorageClass
        value: 'smb'
