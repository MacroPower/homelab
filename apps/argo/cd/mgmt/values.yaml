# yaml-language-server: $schema=../../../../charts/argo_cd/values.schema.json

# https://artifacthub.io/packages/helm/argo/argo-cd

configs:
  ## General Argo CD configuration
  ## Ref: https://github.com/argoproj/argo-cd/blob/master/docs/operator-manual/argocd-cm.yaml
  ##
  ## Resource customizations, exclusions, and compare options are inherited
  ## from base; only hub-specific config (URL, OIDC, RBAC, sizing) lives here.
  cm:
    url: https://argo-cd.jacobcolvin.com
    oidc.config: |
      name: Auth0
      issuer: https://auth.jacobcolvin.com/
      # Reference keys in argocd-secret
      clientID: $argocd-oidc:auth0.clientID
      clientSecret: $argocd-oidc:auth0.clientSecret
      requestedScopes:
        - openid
        - profile
        - email
        - colvinGroups

  ## Argo CD configuration parameters
  ## Ref: https://github.com/argoproj/argo-cd/blob/master/docs/operator-manual/argocd-cmd-params-cm.yaml
  ##
  params:
    server.disable.auth: false

  rbac:
    policy.csv: |
      g, argo-cd-admin, role:admin
    policy.default: role:readonly
    scopes: '[colvinGroups, email]'

controller:
  replicas: 3
  resources:
    requests:
      cpu: 500m
      memory: 2Gi

server:
  replicas: 3
  resources:
    requests:
      cpu: 200m
      memory: 250Mi

repoServer:
  replicas: 2
  resources:
    requests:
      cpu: 500m
      memory: 500Mi

applicationSet:
  replicaCount: 3
  resources: &StdResources
    requests:
      cpu: 50m
      memory: 100Mi

notifications:
  resources: *StdResources

dex:
  resources: *StdResources
  initImage:
    resources: *StdResources
