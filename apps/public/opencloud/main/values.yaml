# yaml-language-server: $schema=../../../../charts/opencloud/values.schema.json

global:
  domain:
    collabora: collabora.main.cin.macro.network
    companion: companion.main.cin.macro.network
    minio: minio.main.cin.macro.network
    onlyoffice: onlyoffice.main.cin.macro.network
    opencloud: opencloud.main.cin.macro.network
    wopi: wopi.main.cin.macro.network

collabora:
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: 1
      memory: "1Gi"

tika:
  resources:
    requests:
      cpu: 100m
      memory: 1Gi
    limits:
      cpu: 1000m
      memory: 3Gi
