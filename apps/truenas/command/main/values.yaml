# yaml-language-server: $schema=../../../../charts/truecommand/values.schema.json

# The 4Gi memory limit is inherited from base; only prod sizing for requests
# lives here.
controllers:
  truecommand:
    containers:
      app:
        resources:
          requests:
            cpu: 100m
            memory: 2Gi
