# yaml-language-server: $schema=../../../../charts/restic/values.schema.json

controllers:
  restic:
    pod:
      nodeSelector:
        kubernetes.io/hostname: knas01

persistence:
  data:
    type: hostPath
    hostPath: /var/archive/backups/repos
    globalMounts:
      - path: /data
