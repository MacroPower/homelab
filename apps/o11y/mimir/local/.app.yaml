# yaml-language-server: $schema=../../../../konfig/models/frontend/patch.schema.json

syncPolicy:
  automated:
    selfHeal: true

# Heavy stack: disabled by default so a stock local bring-up stays within the
# single node's budget. Test it with: task tald:enable APP=o11y/mimir && task tald:push
enabled: false
