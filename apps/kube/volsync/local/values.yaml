# yaml-language-server: $schema=../../../../charts/volsync/values.schema.json

# Docker-safe overrides only; everything else inherited from base.
resources:
  requests:
    cpu: "100m"
    memory: "64Mi"
  limits:
    cpu: "1000m"
    memory: "1Gi"
