# yaml-language-server: $schema=../../../../charts/qbittorrent/values.schema.json

controllers:
  qbittorrent:
    containers:
      app:
        env:
          QBT_TORRENTING_PORT: {value: &torrentPort 50481}

service:
  torrent:
    # type: LoadBalancer
    ports:
      torrent-tcp:
        port: *torrentPort
