"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1


schema CiliumBGPPeeringPolicy:
    """
    CiliumBGPPeeringPolicy is a Kubernetes third-party resource for instructing Cilium's BGP control plane to create virtual BGP routers.

    Attributes
    ----------
    apiVersion : str, default is "cilium.io/v2alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "CiliumBGPPeeringPolicy", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, required
        metadata
    spec : CiliumIoV2alpha1CiliumBGPPeeringPolicySpec, default is Undefined, optional
        spec
    """


    apiVersion: "cilium.io/v2alpha1" = "cilium.io/v2alpha1"

    kind: "CiliumBGPPeeringPolicy" = "CiliumBGPPeeringPolicy"

    metadata: v1.ObjectMeta

    spec?: CiliumIoV2alpha1CiliumBGPPeeringPolicySpec


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpec:
    """
    Spec is a human readable description of a BGP peering policy

    Attributes
    ----------
    nodeSelector : CiliumIoV2alpha1CiliumBGPPeeringPolicySpecNodeSelector, default is Undefined, optional
        node selector
    virtualRouters : [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0], default is Undefined, required
        A list of CiliumBGPVirtualRouter(s) which instructs the BGP control plane how to instantiate virtual BGP routers.
    """


    nodeSelector?: CiliumIoV2alpha1CiliumBGPPeeringPolicySpecNodeSelector

    virtualRouters: [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0]


    check:
        len(virtualRouters) >= 1


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecNodeSelector:
    """
    NodeSelector selects a group of nodes where this BGP Peering Policy applies. 
     If empty / nil this policy applies to all nodes.

    Attributes
    ----------
    matchExpressions : [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecNodeSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecNodeSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


    check:
        all _, matchLabels in matchLabels {len(matchLabels) <= _regex_match(str(matchLabels), r"^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$") if matchLabels } if matchLabels


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecNodeSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: "In" | "NotIn" | "Exists" | "DoesNotExist"

    values?: [str]


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0:
    """
    CiliumBGPVirtualRouter defines a discrete BGP virtual router configuration.

    Attributes
    ----------
    exportPodCIDR : bool, default is Undefined, optional
        ExportPodCIDR determines whether to export the Node's private CIDR block to the configured neighbors.
    localASN : int, default is Undefined, required
        LocalASN is the ASN of this virtual router. Supports extended 32bit ASNs
    neighbors : [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0], default is Undefined, required
        Neighbors is a list of neighboring BGP peers for this virtual router
    podIPPoolSelector : CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0PodIPPoolSelector, default is Undefined, optional
        pod IP pool selector
    serviceAdvertisements : [str], default is ["LoadBalancerIP"], optional
        ServiceAdvertisements selects a group of BGP Advertisement(s) to advertise for the selected services.
    serviceSelector : CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0ServiceSelector, default is Undefined, optional
        service selector
    """


    exportPodCIDR?: bool = False

    localASN: int

    neighbors: [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0]

    podIPPoolSelector?: CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0PodIPPoolSelector

    serviceAdvertisements?: [str] = ["LoadBalancerIP"]

    serviceSelector?: CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0ServiceSelector


    check:
        localASN <= 4.294967295e+09
        localASN >= 0
        len(neighbors) >= 1


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0:
    """
    CiliumBGPNeighbor is a neighboring peer for use in a CiliumBGPVirtualRouter configuration.

    Attributes
    ----------
    advertisedPathAttributes : [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0], default is Undefined, optional
        AdvertisedPathAttributes can be used to apply additional path attributes to selected routes when advertising them to the peer. If empty / nil, no additional path attributes are advertised.
    authSecretRef : str, default is Undefined, optional
        AuthSecretRef is the name of the secret to use to fetch a TCP authentication password for this peer.
    connectRetryTimeSeconds : int, default is 120, optional
        ConnectRetryTimeSeconds defines the initial value for the BGP ConnectRetryTimer (RFC 4271, Section 8).
    eBGPMultihopTTL : int, default is 1, optional
        EBGPMultihopTTL controls the multi-hop feature for eBGP peers. Its value defines the Time To Live (TTL) value used in BGP packets sent to the neighbor. The value 1 implies that eBGP multi-hop feature is disabled (only a single hop is allowed). This field is ignored for iBGP peers.
    families : [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0FamiliesItems0], default is Undefined, optional
        Families, if provided, defines a set of AFI/SAFIs the speaker will negotiate with it's peer. 
         If this slice is not provided the default families of IPv6 and IPv4 will be provided.
    gracefulRestart : CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0GracefulRestart, default is Undefined, optional
        graceful restart
    holdTimeSeconds : int, default is 90, optional
        HoldTimeSeconds defines the initial value for the BGP HoldTimer (RFC 4271, Section 4.2). Updating this value will cause a session reset.
    keepAliveTimeSeconds : int, default is 30, optional
        KeepaliveTimeSeconds defines the initial value for the BGP KeepaliveTimer (RFC 4271, Section 8). It can not be larger than HoldTimeSeconds. Updating this value will cause a session reset.
    peerASN : int, default is Undefined, required
        PeerASN is the ASN of the peer BGP router. Supports extended 32bit ASNs
    peerAddress : str, default is Undefined, required
        PeerAddress is the IP address of the peer. This must be in CIDR notation and use a /32 to express a single host.
    peerPort : int, default is 179, optional
        PeerPort is the TCP port of the peer. 1-65535 is the range of valid port numbers that can be specified. If unset, defaults to 179.
    """


    advertisedPathAttributes?: [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0]

    authSecretRef?: str

    connectRetryTimeSeconds?: int = 120

    eBGPMultihopTTL?: int = 1

    families?: [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0FamiliesItems0]

    gracefulRestart?: CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0GracefulRestart

    holdTimeSeconds?: int = 90

    keepAliveTimeSeconds?: int = 30

    peerASN: int

    peerAddress: str

    peerPort?: int = 179


    check:
        connectRetryTimeSeconds <= 2.147483647e+09
        connectRetryTimeSeconds >= 1
        eBGPMultihopTTL <= 255
        eBGPMultihopTTL >= 1
        holdTimeSeconds <= 65535
        holdTimeSeconds >= 3
        keepAliveTimeSeconds <= 65535
        keepAliveTimeSeconds >= 1
        peerASN <= 4.294967295e+09
        peerASN >= 0
        peerPort <= 65535
        peerPort >= 1


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0:
    """
    CiliumBGPPathAttributes can be used to apply additional path attributes to matched routes when advertising them to a BGP peer.

    Attributes
    ----------
    communities : CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0Communities, default is Undefined, optional
        communities
    localPreference : int, default is Undefined, optional
        LocalPreference defines the preference value advertised in the BGP Local Preference path attribute. As Local Preference is only valid for iBGP peers, this value will be ignored for eBGP peers (no Local Preference path attribute will be advertised). If nil / not set, the default Local Preference of 100 will be advertised in the Local Preference path attribute for iBGP peers.
    selector : CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0Selector, default is Undefined, optional
        selector
    selectorType : str, default is Undefined, required
        SelectorType defines the object type on which the Selector applies: - For "PodCIDR" the Selector matches k8s CiliumNode resources (path attributes apply to routes announced for PodCIDRs of selected CiliumNodes. Only affects routes of cluster scope / Kubernetes IPAM CIDRs, not Multi-Pool IPAM CIDRs. - For "CiliumLoadBalancerIPPool" the Selector matches CiliumLoadBalancerIPPool custom resources (path attributes apply to routes announced for selected CiliumLoadBalancerIPPools). - For "CiliumPodIPPool" the Selector matches CiliumPodIPPool custom resources (path attributes apply to routes announced for allocated CIDRs of selected CiliumPodIPPools).
    """


    communities?: CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0Communities

    localPreference?: int

    selector?: CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0Selector

    selectorType: "PodCIDR" | "CiliumLoadBalancerIPPool" | "CiliumPodIPPool"


    check:
        localPreference <= 4.294967295e+09
        localPreference >= 0


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0Communities:
    """
    Communities defines a set of community values advertised in the supported BGP Communities path attributes. If nil / not set, no BGP Communities path attribute will be advertised.

    Attributes
    ----------
    large : [str], default is Undefined, optional
        Large holds a list of the BGP Large Communities Attribute (RFC 8092) values.
    standard : [str], default is Undefined, optional
        Standard holds a list of "standard" 32-bit BGP Communities Attribute (RFC 1997) values defined as numeric values.
    wellKnown : [str], default is Undefined, optional
        WellKnown holds a list "standard" 32-bit BGP Communities Attribute (RFC 1997) values defined as well-known string aliases to their numeric values.
    """


    large?: [str]

    standard?: [str]

    wellKnown?: [str]


    check:
        all large in large {_regex_match(str(large), r"^([0-9]|[1-9][0-9]{1,8}|[1-3][0-9]{9}|4[01][0-9]{8}|42[0-8][0-9]{7}|429[0-3][0-9]{6}|4294[0-8][0-9]{5}|42949[0-5][0-9]{4}|429496[0-6][0-9]{3}|4294967[01][0-9]{2}|42949672[0-8][0-9]|429496729[0-5]):([0-9]|[1-9][0-9]{1,8}|[1-3][0-9]{9}|4[01][0-9]{8}|42[0-8][0-9]{7}|429[0-3][0-9]{6}|4294[0-8][0-9]{5}|42949[0-5][0-9]{4}|429496[0-6][0-9]{3}|4294967[01][0-9]{2}|42949672[0-8][0-9]|429496729[0-5]):([0-9]|[1-9][0-9]{1,8}|[1-3][0-9]{9}|4[01][0-9]{8}|42[0-8][0-9]{7}|429[0-3][0-9]{6}|4294[0-8][0-9]{5}|42949[0-5][0-9]{4}|429496[0-6][0-9]{3}|4294967[01][0-9]{2}|42949672[0-8][0-9]|429496729[0-5])$") if large } if large
        all standard in standard {_regex_match(str(standard), r"^([0-9]|[1-9][0-9]{1,8}|[1-3][0-9]{9}|4[01][0-9]{8}|42[0-8][0-9]{7}|429[0-3][0-9]{6}|4294[0-8][0-9]{5}|42949[0-5][0-9]{4}|429496[0-6][0-9]{3}|4294967[01][0-9]{2}|42949672[0-8][0-9]|429496729[0-5])$|^([0-9]|[1-9][0-9]{1,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5]):([0-9]|[1-9][0-9]{1,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])$") if standard } if standard


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0Selector:
    """
    Selector selects a group of objects of the SelectorType resulting into routes that will be announced with the configured Attributes. If nil / not set, all objects of the SelectorType are selected.

    Attributes
    ----------
    matchExpressions : [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0SelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0SelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


    check:
        all _, matchLabels in matchLabels {len(matchLabels) <= _regex_match(str(matchLabels), r"^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$") if matchLabels } if matchLabels


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0AdvertisedPathAttributesItems0SelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: "In" | "NotIn" | "Exists" | "DoesNotExist"

    values?: [str]


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0FamiliesItems0:
    """
    CiliumBGPFamily represents a AFI/SAFI address family pair.

    Attributes
    ----------
    afi : str, default is Undefined, required
        Afi is the Address Family Identifier (AFI) of the family.
    safi : str, default is Undefined, required
        Safi is the Subsequent Address Family Identifier (SAFI) of the family.
    """


    afi: "ipv4" | "ipv6" | "l2vpn" | "ls" | "opaque"

    safi: "unicast" | "multicast" | "mpls_label" | "encapsulation" | "vpls" | "evpn" | "ls" | "sr_policy" | "mup" | "mpls_vpn" | "mpls_vpn_multicast" | "route_target_constraints" | "flowspec_unicast" | "flowspec_vpn" | "key_value"


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0NeighborsItems0GracefulRestart:
    """
    GracefulRestart defines graceful restart parameters which are negotiated with this neighbor. If empty / nil, the graceful restart capability is disabled.

    Attributes
    ----------
    enabled : bool, default is Undefined, required
        Enabled flag, when set enables graceful restart capability.
    restartTimeSeconds : int, default is 120, optional
        RestartTimeSeconds is the estimated time it will take for the BGP session to be re-established with peer after a restart. After this period, peer will remove stale routes. This is described RFC 4724 section 4.2.
    """


    enabled: bool

    restartTimeSeconds?: int = 120


    check:
        restartTimeSeconds <= 4095
        restartTimeSeconds >= 1


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0PodIPPoolSelector:
    """
    PodIPPoolSelector selects CiliumPodIPPools based on labels. The virtual router will announce allocated CIDRs of matching CiliumPodIPPools. 
     If empty / nil no CiliumPodIPPools will be announced.

    Attributes
    ----------
    matchExpressions : [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0PodIPPoolSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0PodIPPoolSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


    check:
        all _, matchLabels in matchLabels {len(matchLabels) <= _regex_match(str(matchLabels), r"^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$") if matchLabels } if matchLabels


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0PodIPPoolSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: "In" | "NotIn" | "Exists" | "DoesNotExist"

    values?: [str]


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0ServiceSelector:
    """
    ServiceSelector selects a group of load balancer services which this virtual router will announce. The loadBalancerClass for a service must be nil or specify a class supported by Cilium, e.g. "io.cilium/bgp-control-plane". Refer to the following document for additional details regarding load balancer classes: 
     https://kubernetes.io/docs/concepts/services-networking/service/#load-balancer-class 
     If empty / nil no services will be announced.

    Attributes
    ----------
    matchExpressions : [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0ServiceSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0ServiceSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


    check:
        all _, matchLabels in matchLabels {len(matchLabels) <= _regex_match(str(matchLabels), r"^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$") if matchLabels } if matchLabels


schema CiliumIoV2alpha1CiliumBGPPeeringPolicySpecVirtualRoutersItems0ServiceSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: "In" | "NotIn" | "Exists" | "DoesNotExist"

    values?: [str]


